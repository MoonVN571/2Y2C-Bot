module.exports = {
    name: "test",
    description: "command.",
    aliases: ['test'],
    
    async execute(client, message, args) {
        
    //     var abc = require("../api")
    //     var api = new abc();

    //    message.channel.send(api.removeFormat("_test_")).then(() => {
            
    //     message.channel.send(api.removeFormat("_test12_"));
    //    })
    }
}